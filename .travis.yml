sudo: required
services:
  - docker

before_install:
  - docker build -t dorianadret/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true -t dorianadret/docker-react yarn test -- --coverage
